version: '3.8'

services:
  resume-web:
    image: alikaan-resume:latest
    container_name: resume-website
    environment:
      - VIRTUAL_HOST=alikaanturkmen.com,www.alikaanturkmen.com
      - LETSENCRYPT_HOST=alikaanturkmen.com,www.alikaanturkmen.com
      - LETSENCRYPT_EMAIL=turkmenalikaan@gmail.com
    expose:
      - "80"
    restart: unless-stopped
    networks:
      - resume-network

  nginx-proxy:
    image: nginxproxy/nginx-proxy
    container_name: nginx-proxy
    labels:
      - "com.github.nginx-proxy.nginx"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - certs:/etc/nginx/certs
      - vhost:/etc/nginx/vhost.d
      - html:/usr/share/nginx/html
    networks:
      - resume-network
    restart: unless-stopped

  letsencrypt:
    image: nginxproxy/acme-companion
    container_name: letsencrypt
    volumes_from:
      - nginx-proxy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - certs:/etc/nginx/certs
      - acme:/etc/acme.sh
    environment:
      - DEFAULT_EMAIL=turkmenalikaan@gmail.com
      - NGINX_PROXY_CONTAINER=nginx-proxy
    depends_on:
      - nginx-proxy
    networks:
      - resume-network
    restart: unless-stopped

networks:
  resume-network:
    driver: bridge

volumes:
  certs:
  vhost:
  html:
  acme: